# ── 服务端 AI 共享密钥（可选）────────────────────────────────────────
#
# 默认全部留空。这是推荐状态，不是"还没配好"：
#   · 服务端不持有任何密钥
#   · 应用自动走公开翻译降级链路，功能完整可用
#   · 每个用户在「个人中心 → 智能翻译引擎配置」里填自己的密钥，
#     密钥只存在他自己的浏览器里，不经过你
#   · 因此把这个网址公开分享也是安全的 —— 访客烧不到你的额度
#
# 什么时候才需要填：
#   只有当你自己部署、并且希望所有访客共用你的密钥时才填。
#   ⚠️ 一旦填了，任何拿到网址的人都能消耗你的额度。此时务必配置 SERVER_API_ACCESS_TOKEN 保护。
#
# 密钥只从环境变量读取，不写入磁盘、不进日志（server.ts 有脱敏处理）。
GEMINI_API_KEY=""
DEEPSEEK_API_KEY=""
OPENAI_COMPATIBLE_API_KEY=""

# ── 服务端口与地址 ───────────────────────────────────────────────────
# 本地服务监听端口，默认 3000
PORT="3000"

# 用于生成自引用链接。本地开发填 http://localhost:3000，部署后填实际域名。
APP_URL="http://localhost:3000"

# ── 公网部署保护 ───────────────────────────────────────────────────
# 配置后，使用服务端共享 AI 密钥的请求必须携带 X-LingoLog-Access-Token。
# 不配置服务端 AI 密钥时可以留空，访客仍可使用离线降级和自己的浏览器密钥。
SERVER_API_ACCESS_TOKEN=""

# 外部助手收件箱默认关闭。只有明确配置 ENABLE_INBOX=true 且设置 INBOX_TOKEN 时才启用。
ENABLE_INBOX="false"
INBOX_TOKEN=""
